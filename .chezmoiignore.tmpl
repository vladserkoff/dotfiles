{{ if ne .chezmoi.os "darwin" }}
{{/* macOS-only targets: GUI app configs and system-level setup */}}
.Brewfile
.config/zed
.macos
.mutagen.yml
Library
{{ end }}
{{/* repo-only files that must never land in $HOME */}}
CLAUDE.md
readme.md
scripts
